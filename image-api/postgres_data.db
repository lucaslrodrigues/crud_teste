use userData;

create table users(
	id serial primary key,
	name varchar(45),
	login varchar(45),
	senha varchar(45)
);

select * from users;
truncate table users;

insert into users values (default, 'lucas', 'lucas@email', '12345678');